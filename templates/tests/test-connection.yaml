apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spinkube-oneclick.fullname" . }}-test-connection"
  labels:
    {{- include "spinkube-oneclick.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spinkube-oneclick.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
